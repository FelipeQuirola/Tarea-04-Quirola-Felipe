r2, i = bisection(
    a = 1, 
    b = 3.2,
    equation = lambda x: x**3 - 7*x**2 + 14*x - 6, 
    tol = 10**(-2), 
    N = 20
)
print(f'La bisección dio como resultado la raíz {r2} en {i} iteraciones')
